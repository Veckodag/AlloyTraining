@using EPiServer.Core
@model AlloyTraining.Models.Pages.SitePageData

<div class="block span4">
    <div class="border">
     @if (Model.HasTemplate())
     {
         <a href="@Url.PageUrl(Model.LinkURL)">
             <h2>@Model.PageName</h2>
             <p>@Html.PropertyFor(x => x.TeaserText)</p>
             <img src="@Url.ContentUrl(Model.PageImage)" alt="PageImage"/>
         </a>
     }
     else
     {
         <h2>@Model.PageName</h2>
         <p>@Html.PropertyFor(x => x.TeaserText)</p>
         <img src="@Url.ContentUrl(Model.PageImage)" alt="PageImage"/>
     }
    </div>
</div>
